from PyPDF2 import PdfFileMerger

#List of PDFs to be merged into a single file named: result.pdf
pdfs=['file1.pdf','file2.pdf']

merger=PdfFileMerger()

for pdf in pdfs:
    merger.append(open(pdf,'rb'))

with open('result.pdf', 'wb') as fout:
    merger.write(fout)
